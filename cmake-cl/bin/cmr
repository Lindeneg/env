#!/usr/bin/bash

# a stupid wrapper that runs an executable of the
# same name as the folder where that executable resides
# i.e ~/hello/there/kenobi/ -> ./bin/kenobi.exe

FN="$(basename "$PWD")"

"$PWD"/bin/"$FN".exe
